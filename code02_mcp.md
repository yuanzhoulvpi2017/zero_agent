## MCP
1. 介绍agent的时候，需要一些tool：比如mcp或者skill。
2. 这里启动一个服务，提供一些现成的mcp。



## 安装和启动
1. 安装和配置python环境`uv sync`
2. 启动`uv run main.py`
3. 启动后，如果希望使用mcp，可以直接把链接放进去`http://192.168.0.106:8123/mcpserver/simple_tool/mcp` 这里的是`192.168.0.106` 是本地ip地址。换成自己的即可。