# 仓库开发约定

本文件适用于整个仓库，供后续编程 Agent 在新增或修改代码时遵循。业务开发说明见 [docs/README.md](docs/README.md)。

## 目录与业务组织

- 采用阶段目录：`agent/`、`dataset/`、`training/`、`evaluation/`、`interface/`、`configs/`、`docs/`。新增业务放在各阶段的业务子目录中，不另建 `projects/<业务名>/` 结构。
- 同一业务在所有阶段使用相同的小写英文加下划线名称。按实际开发进度创建目录，不预建无实现的业务或阶段。
- 保留现有根目录基础教程、示例及其运行方式；调整布局时同步修改受影响的导入、路径和文档。
- 每个业务以 `docs/<业务名>/README.md` 为总入口，说明目标、任务示例、阶段进度、代码链接与运行方式。

| 路径 | 职责 |
| --- | --- |
| `agent/<业务名>/` | Agent 实现、自定义工具、提示词、轨迹采集 |
| `dataset/<业务名>/` | 轨迹清洗、过滤、标注、数据划分、SFT 格式转换 |
| `training/<业务名>/` | 训练入口、模型导出 |
| `evaluation/<业务名>/` | 推理服务、批量评测、效果对比、报告生成 |
| `interface/<业务名>/` | 所有用户交互界面与后端调用适配 |
| `configs/<业务名>/` | 采集、数据处理、训练、评测配置 |
| `docs/<业务名>/` | 业务导航和分阶段教程 |
| `artifacts/<业务名>/<实验编号>/` | 原始轨迹、数据集、模型权重、运行记录和报告 |

## 依赖与代码边界

- 根目录的 `pyproject.toml` 和 `uv.lock` 用于现有基础教程。
- 新的 Python 阶段模块首次引入可运行代码时，在阶段目录中管理自己的 `pyproject.toml`、`uv.lock` 和虚拟环境。训练环境独立管理；同一阶段出现业务依赖冲突时，再为对应业务建立独立环境。
- 所有使用 AgentScope 的模块必须声明 `agentscope>=1.0.18,<2.0.0`，禁止引入 AgentScope 2.x 及以上版本。修改依赖时同步更新对应锁文件。
- 各阶段通过明确的文件格式或服务接口衔接，文档说明输入、输出及路径解析方式，避免依赖隐含的工作目录或其他阶段的虚拟环境。
- 出现稳定的重复需求后再提取阶段内的 `common/`，不要提前构建公共框架。

## 用户交互层

- 后续所有面向用户的交互界面统一放在 `interface/<业务名>/`，包括聊天、任务提交、轨迹浏览、数据标注、训练状态查看和评测结果展示，按实际需求实现。
- `interface/` 负责界面、交互状态和后端调用适配；Agent 执行与工具实现属于 `agent/`，数据处理属于 `dataset/`，训练属于 `training/`，模型推理服务和评测计算属于 `evaluation/`。界面调用这些模块提供的明确接口，不复制核心业务逻辑。
- 各阶段可以保留独立的脚本、批处理入口和服务接口；需要新增用户交互界面时统一放到 `interface/`。现有基础教程示例保持原有组织方式。
- 界面可随任一阶段开发，无需等待训练或评测完成。业务文档写明界面启动方式、依赖的后端服务、配置和接口约定。
- 界面依赖按所选技术栈独立管理：Python 界面使用自己的 `pyproject.toml` 和 `uv.lock`；JavaScript/TypeScript 界面使用 `package.json` 和对应锁文件。尚未选型时不预建框架或依赖。

## 数据与实验管理

- 四个阶段长期共存，支持反复执行“采集 → 数据处理 → 训练 → 评测”。Git 提交和标签记录开发版本，不替代阶段模块。
- 原始轨迹与训练样本分开保存。轨迹保留消息、工具调用及返回、执行状态，以及模型和配置标识，支持重新处理。
- `dataset/` 保存数据处理代码，运行产物写入 `artifacts/<业务名>/<实验编号>/`，不提交 Git，不覆盖旧实验。
- 每次实验保存 `manifest.json`，记录代码提交、实际配置、轨迹批次、数据集版本、基座模型、训练产物、测试集版本及相关产物路径。尚未执行的阶段不填写虚构结果。
- 测试任务提前留出，避免进入训练数据。对比商业模型、未训练基座模型和 SFT 模型接入 Agent 后的效果时，尽量保持测试任务、工具、提示词和执行限制一致，并记录差异。
- 评测关注任务成功率、工具调用正确率、耗时和成本；明确指标计算方式。
- 真实密钥通过环境变量或本地 `.env` 提供，不写入源码、提交的配置、轨迹或实验记录。配置模板使用不含真实密钥的 `.env.example`。

## 文档与验证

- 阶段文档按需使用 `01_agent.md`、`02_dataset.md`、`03_training.md`、`04_evaluation.md`，界面文档使用 `05_interface.md`（编号仅用于导航，不限制开发顺序），说明解决的问题、环境准备、执行目录、运行命令、输入输出格式和预期结果。
- 用 Markdown 勾选清单跟踪阶段内任务；新增入口、修改配置或数据格式时，同步更新业务文档。
- 修改后执行与变更相关的验证。纯目录或文档调整检查路径、链接和差异格式；代码修改执行适当的运行检查或测试。不能执行的检查要说明原因，不宣称通过。
